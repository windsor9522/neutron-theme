<article style="padding:120px 0 80px;">
  <div class="container" style="max-width:760px;">
    <h1 style="font-family:var(--font-heading);font-size:48px;font-weight:900;text-transform:uppercase;margin-bottom:24px;line-height:1.1;">{{ page.title }}</h1>
    <div style="font-size:16px;color:var(--muted);line-height:1.8;">{{ page.content }}</div>
  </div>
</article>
